diff -Nru lxpanel-0.9.3.orig/plugins/batt/batt_sys.c lxpanel-0.9.3/plugins/batt/batt_sys.c
--- lxpanel-0.9.3.orig/plugins/batt/batt_sys.c	2017-01-20 22:34:34.000000000 +0100
+++ lxpanel-0.9.3/plugins/batt/batt_sys.c	2017-03-02 18:26:09.530211690 +0100
@@ -254,9 +254,9 @@
     }
 #endif
 
-    if (b->charge_now != -1 && b->charge_full != -1)
+    if (b->charge_now != -1 && b->charge_full != -1 && b->charge_full != 0)
         promille = (b->charge_now * 1000) / b->charge_full;
-    else if (b->energy_full != -1 && b->energy_now != -1)
+    else if (b->energy_full != -1 && b->energy_now != -1 && b->energy_full != 0)
         /* no charge data, let try energy instead */
         promille = (b->energy_now * 1000) / b->energy_full;
     else
